<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yts8.sixuniverse.member.repository.MemberMapper">
  <select id="findByEmail" resultType="Member">
    select * from member where email=#{email}
  </select>

  <insert id="save" parameterType="Member">
    insert into member (email, password, name, birthdate, zipcode, address, sub_address, phone, social, role)
    values (#{email}, #{password}, #{name}, #{birthdate}, #{zipcode}, #{address}, #{subAddress}, #{phone}, #{social}, #{role})
  </insert>

</mapper>
